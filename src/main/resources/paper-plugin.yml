name: MortisBank
version: '5.0.0'
main: com.mortisdevelopment.mortisbank.MortisBank
api-version: '1.20'
dependencies:
  server:
    MortisCore:
      load: BEFORE
      required: true
      join-classpath: true
    Vault:
      load: BEFORE
      required: true
      join-classpath: true
    PlaceholderAPI:
      load: BEFORE
      required: true
      join-classpath: true
    Essentials:
      load: BEFORE
      required: false
      join-classpath: false